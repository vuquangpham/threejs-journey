{
  "version": 2,
  "builds": [
    {
      "src": "index.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/"
    }
  ],
  "env": {
    "BRANCH": "vercel"
  },
  "scripts": {
    "vercel-deploy": "if [ \"$BRANCH\" != \"master\" ]; then echo \"Cannot deploy from $BRANCH branch\"; exit 1; fi"
  }
}
